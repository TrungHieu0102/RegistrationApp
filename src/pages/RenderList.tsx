import { Link } from "react-router-dom"

const RenderList = () => {
  return (
    <div>
        <h1>Render List</h1>
        <Link to="/">Back to home page</Link>

    </div>
  )
}

export default RenderList